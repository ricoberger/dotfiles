#!/usr/bin/env bash

set -e

KUBECONFIG=$(for f in `fd --full-path --hidden --color never --type f -e yaml "" "${HOME}/Documents/kubeconfig/"`; do echo -n "${f}:"; done;) kubectl config view --merge --flatten > ~/.kube/config
