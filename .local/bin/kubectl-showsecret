#!/usr/bin/env bash

set -e

secret=$(kubectl get secret --no-headers -o custom-columns=":metadata.name" $@ | fzf)
kubectl get secret $secret $@ -o go-template='{{range $k,$v := .data}}{{printf "%s: " $k}}{{if not $v}}{{$v}}{{else}}{{$v | base64decode}}{{end}}{{"\n"}}{{end}}'
